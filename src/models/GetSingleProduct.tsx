import { ProductType } from '../types';

export const GetASingleProduct = (id: number) => {
	// let data: ProductType = [];
	// let loading: boolean = true;
	// fetch(`https://fakestoreapi.com/products/${id}`)
	// 	.then((res) => res.json())
	// 	.then((json) => (data = json))
	// 	.catch((err) => console.log(err))
	// 	.finally(() => (loading = false));
	// return data;
};
